repositories {
    jcenter()
    maven { url 'https://jitpack.io' }
}

apply plugin: "kotlin"
apply plugin: "application"

mainClassName = "MainKt"

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib:$kotlin_version"
    implementation project(':DIP-checker')
}
